## 1. 接口契约

- [x] 1.1 定义分析任务创建、状态查询、标准化结果获取和展示模型获取的 API 契约
- [x] 1.2 固定分析任务状态集合、错误响应结构和基础会话字段
- [x] 1.3 固定豆包调用配置只使用 `ARK_API_KEY`、`ARK_ENDPOINT_ID` 和 `ARK_BASE_URL`

## 2. 意图识别契约

- [x] 2.1 定义自然语言意图识别接口的输入输出结构
- [x] 2.2 确定第一版受控意图集合、回退策略和低置信度处理规则

## 3. 数据结构

- [x] 3.1 定义标准化分析结果 schema，覆盖基础元数据、脚本结构、节奏结构、置信度和可用性状态
- [x] 3.2 定义前端展示模型 schema，覆盖时间轴片段、右侧结构卡片和联动字段

## 4. 前后端分离落地

- [x] 4.1 明确前端只依赖业务能力 API、不感知 provider 和模型厂商
- [x] 4.2 明确前端轮询、任务超时和失败提示的交互约定
- [x] 4.3 明确后端当前统一通过 HTTP `chat/completions` 调用豆包模型，并在服务端完成结果标准化
