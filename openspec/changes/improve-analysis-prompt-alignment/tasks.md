## 1. API 能力确认与契约

- [x] 1.1 明确当前 `.env` 对应 endpoint 支持的真实视频输入协议
- [x] 1.2 定义样例分析任务的 `analysis_instruction` 与视频输入契约
- [x] 1.3 为标准结果与 view-model 定义统一 provider trace 和 `input_modality`

## 2. 后端真实视频分析链路

- [x] 2.1 扩展模型 provider，使其支持真实视频输入而不只是纯文本 `chat/completions`
- [x] 2.2 更新 `ReferenceAnalyzerAgent`，让其基于真实视频输入与分析指令发起请求
- [x] 2.3 保留并适配 JSON 提取、校验与修复逻辑到真实视频分析结果
- [x] 2.4 为“不支持视频输入”“上传失败”“模型拒绝处理”等场景增加显式错误处理

## 3. 降级与透明度

- [x] 3.1 将 heuristic 路径调整为显式降级，而不是默认等价替代
- [x] 3.2 在结果标准化层输出统一 provider trace、fallback 状态和输入形态

## 4. 前端与调试可见性

- [x] 4.1 在工作台中展示当前 provider、真实视频输入形态、分析指令和关键 warning
- [x] 4.2 在调试视图中保留最小化但足够判断“是否真的分析了视频”的诊断信息

## 5. 文档与验证

- [x] 5.1 更新联调文档，说明如何判断当前结果是否来自真实视频分析 API
- [x] 5.2 增加验证脚本或验证步骤，覆盖真实视频分析成功、能力不支持和显式降级三类场景
